---
paths:
  - "**"
---

# Git Process

- Branches: `feature/name`, `fix/name`.
- Commits: conventional commits in English — `feat(scope): description`.
- Before committing: `npm run build` and `npm run typecheck`.
- Never commit directly to `main`.
